export const metadata = {
  title: "ESP32 RFID Reader",
  description: "Real-time RFID reader interface for ESP32",
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
